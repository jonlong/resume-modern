---
export interface Props {
  url: string;
  class?: string;
}

const { url, class: className } = Astro.props;
---

<a href={url} class:list={["hover:text-sky-600 hover:underline", className]}>
  <slot />
</a>
